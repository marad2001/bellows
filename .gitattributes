# Default: let git handle line endings per-platform.
* text=auto

# Files that run inside Linux containers must keep LF endings even on
# Windows checkouts, otherwise the shebang/COPY pulls in `\r` characters
# and the container fails at runtime.
policy-image/Dockerfile text eol=lf
policy-image/run-agent text eol=lf
policy-image/run-cargo-checks text eol=lf
policy-image/entrypoint text eol=lf
policy-image/CLAUDE.md text eol=lf
